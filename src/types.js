// @flow

/** Single bit */
export type BitType = 0 | 1;

/** Byte-length number (0x00 - 0xff) */
export type ByteType = number;

/** Byte-length number (0x0000 - 0xffff) */
export type WordType = number;

/** Byte-length number (0x00 - 0xff) */
export type AddrType = number;

/** Byte-length number (0x00 - 0xff) */
export type CmdType = number;
